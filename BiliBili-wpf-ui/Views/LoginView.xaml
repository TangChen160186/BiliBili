<UserControl x:Class="BiliBili_wpf_ui.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BiliBili_wpf_ui.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 标题 -->
        <TextBlock Text="哔哩哔哩账号登录" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Margin="0,30,0,20"/>
        
        <!-- 登录表单 -->
        <Grid Grid.Row="1" Width="300" VerticalAlignment="Top">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- 已登录状态 -->
            <StackPanel Grid.Row="0" 
                        Visibility="{Binding IsLoggedIn, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Image Source="{Binding CurrentUser.AvatarUrl}" 
                       Width="80" 
                       Height="80" 
                       Margin="0,0,0,10"/>
                <TextBlock Text="{Binding CurrentUser.Username}" 
                           FontSize="18" 
                           FontWeight="Bold" 
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,5"/>
                <TextBlock Text="{Binding CurrentUser.Signature}" 
                           TextWrapping="Wrap" 
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,10"/>
                <Button Content="退出登录" 
                        Command="{Binding LogoutCommand}" 
                        Width="100" 
                        Margin="0,10,0,0"/>
            </StackPanel>
            
            <!-- 未登录状态 -->
            <StackPanel Grid.Row="0" 
                        Visibility="{Binding IsLoggedIn, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverse}">
                <!-- 用户名 -->
                <TextBlock Text="用户名" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                         Padding="5" 
                         Margin="0,0,0,15"/>
                
                <!-- 密码 -->
                <TextBlock Text="密码" Margin="0,0,0,5"/>
                <PasswordBox x:Name="PasswordBox" 
                             Padding="5" 
                             Margin="0,0,0,15"/>
                
                <!-- 错误信息 -->
                <TextBlock Text="{Binding ErrorMessage}" 
                           Foreground="Red" 
                           TextWrapping="Wrap" 
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" 
                           Margin="0,0,0,15"/>
                
                <!-- 登录按钮 -->
                <Button Content="登录" 
                        Command="{Binding LoginCommand}" 
                        IsEnabled="{Binding IsLoggingIn, Converter={StaticResource BooleanInverseConverter}}" 
                        Padding="10,5" 
                        HorizontalAlignment="Center"/>
                
                <!-- 加载指示器 -->
                <ProgressBar IsIndeterminate="True" 
                             Visibility="{Binding IsLoggingIn, Converter={StaticResource BooleanToVisibilityConverter}}" 
                             Height="2" 
                             Margin="0,10,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl> 